<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- SQL Guard HikariCP module logging -->
    <logger name="com.footstone.sqlguard.interceptor.jdbc.hikari" level="DEBUG"/>
    
    <!-- SQL Guard common module logging -->
    <logger name="com.footstone.sqlguard.interceptor.jdbc.common" level="DEBUG"/>
    
    <!-- Reduce HikariCP logging noise during tests -->
    <logger name="com.zaxxer.hikari" level="WARN"/>
    
    <!-- H2 database logging -->
    <logger name="org.h2" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>








