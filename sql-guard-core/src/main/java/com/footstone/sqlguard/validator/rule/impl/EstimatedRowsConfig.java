package com.footstone.sqlguard.validator.rule.impl;

import com.footstone.sqlguard.validator.rule.CheckerConfig;

/**
 * Configuration for EstimatedRowsChecker (placeholder/stub).
 * TODO: Implement full configuration in future task.
 */
public class EstimatedRowsConfig extends CheckerConfig {

  public EstimatedRowsConfig() {
    super();
  }

  public EstimatedRowsConfig(boolean enabled) {
    super(enabled);
  }
}

